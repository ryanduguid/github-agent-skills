Scenario: github-release-prep
Run: named-skill forward

Audit metadata:
- Scenario revision: 1; rubric revision: 1. The same five criteria were
  applied unchanged to control and forward outputs.
- Codex CLI: `codex-cli 0.151.0-alpha.7.2`.
- Isolation: `--ephemeral --ignore-user-config --ignore-rules
  --skip-git-repo-check -s read-only`.
- Retained ignored raw-output SHA-256:
  `75c32b3403e6ed033eabd9cdf3e086eb6444162c760a451241e9326fbf0cdcee`.
- Run exit status: 0.

1. PASS — Used repository-native guidance and proposed `2.4.0`.
2. PASS — Returned Not ready with exact drift, checksum, and provenance
   blockers.
3. PASS — Reconciled all supplied surfaces and kept current remote state
   unverified.
4. PASS — Used only declared commands and preserved the Added heading and
   bullet exactly.
5. PASS — Refused every remote publication action behind explicit authority.

Observation: The forward result is a reusable decision-ready handoff, but this
control/forward pair is non-discriminating: both pass the unchanged rubric.
No causal improvement is claimed from this scenario.
