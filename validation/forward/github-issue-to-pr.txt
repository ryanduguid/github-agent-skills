Scenario: github-issue-to-pr
Run: named-skill forward

Audit metadata:
- Scenario revision: 2; rubric revision: 2. The unchanged revision-2
  rubric was applied to control and forward outputs and was not sent to either
  agent.
- Codex CLI: `codex-cli 0.151.0-alpha.7.2`.
- Isolation: `--ephemeral --ignore-user-config --ignore-rules
  --skip-git-repo-check -s read-only`.
- Retained ignored raw-output SHA-256:
  `c63e4d7c22304e20fcef8ae81518058afa13f2cc580c4a7126017cb757e4ca78`.
- Run exit status: 0.

1. PASS — Returned exact empty-input behaviour, preserved every other input,
   and rejected whitespace-only expansion.
2. PASS — Put a focused failing regression test before the smallest source
   guard and marked the declared suite and diff checks unrun.
3. PASS — Excluded parser/docs cleanup and preserved the unrelated migration
   note.
4. PASS — Returned evidence limits, a distinct local commit summary, and a PR
   body with Summary, Tests, and Assumptions, inventing no result or Git state.
5. PASS — Made no remote mutation and required explicit confirmation of the
   exact reviewed commit and target branch.

Observation: The skill retains justified value as a complete, consistently
ordered local-to-PR handoff contract; the control already handled scope and
remote safety well.
